services:
  - type: web
    name: api-toolkit
    runtime: docker
    plan: free
    envVars:
      - key: API_KEY
        generateValue: true
      - key: RATE_LIMIT
        value: 100
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: 8000
    healthCheckPath: /health
